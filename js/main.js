/**
 * Main entry point for the portfolio site.
 * Logic (form validation, language toggle) will be added in later phases.
 *
 * @returns {void}
 */
function init() {
  // Intentionally empty – base structure only.
}

init();
